# CalcChineseCalendar Docusaurus Site

This directory contains the standalone Docusaurus documentation site for the repository.

## Commands

```bash
npm install
npm run start
```

The development server is configured to use port `22202`.
